run:
  timeout: 60s

issues:
  exclude-dirs:
    - vendor
  max-issues-per-linter: 0
  max-same-issues: 0

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
    - revive
    - whitespace
  disable:
    - paralleltest

linters-settings:
  gofmt:
    simplify: true
  goimports:
    local-prefixes: github.com/itsivali/nixos-infrastructure
  revive:
    rules:
      - name: exported
        severity: warning
      - name: package-comments
        disabled: true
