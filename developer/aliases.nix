##############################################################################
#
# Developer Aliases
#
# Purpose
# -------
# NixOS-level developer utilities. Aliases are in
# home/shell/aliases/development.nix (Home Manager).
#
# This module provides system-level environment overrides if needed.
# It exists to satisfy auto-import but currently has no direct config.
#
##############################################################################

{ ... }:

{
  # Developer aliases are managed in home/shell/aliases/development.nix
  # (Home Manager module). This file is reserved for system-level
  # developer environment overrides if needed in the future.
}
