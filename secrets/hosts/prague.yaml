# SOPS secrets for prague
#
# Encrypt with: sops --encrypt --age <age-public-key> secrets/hosts/prague.yaml
#
# Example structure:
# ssh_authorized_keys:
#   - "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI... user@device"
# tailscale_authkey: "tskey-auth-..."
# gitlab_runner_token: "glrt-..."