# Create this file with sops before enabling an unattended switch:
#
#   sops secrets/tailscale.yaml
#
# Expected key:
#
#   tailscale_authkey: your-encrypted-tailscale-auth-key
#
# This placeholder keeps the repository layout explicit. Replace it with an
# encrypted SOPS document before deploying the host.
tailscale_authkey: ENC[AES256_GCM,data:replace-with-sops-encrypted-value,type:str]
