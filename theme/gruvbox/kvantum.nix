##############################################################################
#
# Theme — Gruvbox Kvantum
#
# Purpose
# -------
# Kvantum theme colors for full Gruvbox Qt styling (Phase 3). Consumed by
# the Kvantum theme generator.
#
##############################################################################

{ colors }:

{
  window = colors.bg;
  base = colors.bg1;
  altBase = colors.bg2;
  text = colors.fg;
  button = colors.bg1;
  buttonText = colors.fg;
  highlight = colors.orange;
  highlightedText = colors.bgHard;
  accent = colors.orange;
  inactive = colors.gray;
}
